# Example environment variables

# Backend URL
BACKEND_URL=http://localhost:3001

# Auth0 Configuration
AUTH0_SECRET=your_auth0_secret
AUTH0_BASE_URL=http://localhost:3001 # Or your app's URL
AUTH0_CLIENT_ID=your_auth0_client_id
AUTH0_CLIENT_SECRET=your_auth0_client_secret # If applicable
AUTH0_ISSUER_BASE_URL=https://your-auth0-domain.auth0.com

# MongoDB Configuration (check db.js for the exact variable name expected)
MONGODB_URI=mongodb://localhost:27017/your_database_name

# Port (Optional, defaults to 3000 if not set)
PORT=3001